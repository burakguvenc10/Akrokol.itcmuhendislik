import 'package:flutter/material.dart';

class Splash_model{
  final String asset;
  final String title;

  const Splash_model(
        {
          required this.asset,
          required this.title,
        }
      );

}